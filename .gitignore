# Real config and secrets — never committed. See the *.example / *.template files for the format.
/.env
/.env.bak
/athlete_config.py
# Kept ignored for people migrating from the retired workout DSL.
/workout_plan.py

# Local coaching state and data — never committed.
/coaching/
/data/
/*.csv
/feedback/
/garmin_tokens/
/credentials.json
/token.json

# Python
**/__pycache__/
*.pyc
/.venv/
/.venv-whisper/

# Agent working notes and research are local, not product documentation.
/docs/
